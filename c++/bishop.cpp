#include <iostream>
#include <cmath>
#include <vector>

using namespace std;

int main(){

	int num, amount;
	vector<int> numeros;
	while(cin >>num){
		if (num /1)
			cout << (num-1)*2 << endl;
		else 
			cout << num << endl;
		}
		
	
}
